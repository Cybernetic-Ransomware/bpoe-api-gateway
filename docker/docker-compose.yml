services:
  app:
    build:
      context: ../
      dockerfile: ./docker/Dockerfile
    image: am/bpoe-api-gateway:latest
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/src
    restart: unless-stopped
    ports:
      - "8080:8080"
